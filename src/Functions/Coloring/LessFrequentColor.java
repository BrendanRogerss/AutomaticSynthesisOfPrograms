package Functions.Coloring;

import Graph.Vertex;

/**
 * Created by Brendan on 9/09/2017.
 * It assigns a less frequent feasible
 color in the graph to vertex v. If this is not possible, it assigns a
 feasible color that does not generate conflicts using Greedy (v). If
 the function changes the color assigned to the vertex, it returns
 true; otherwise, it returns false
 */
public class LessFrequentColor extends Coloring{

    public boolean run(Vertex v){

    }
}
